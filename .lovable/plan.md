

# Plano: Converter gráfico de Distribuição por Serviço para pizza com cores da marca

## Alteração

### `MarketingServiceDistribution.tsx`
- Substituir `BarChart` horizontal por `PieChart` com `innerRadius` (donut)
- Usar cores da marca: `hsl(var(--primary))`, `hsl(var(--chart-1))` a `hsl(var(--chart-5))`, `hsl(var(--secondary))`
- Labels com nome do serviço + percentual
- Tooltip formatado com quantidade de leads

